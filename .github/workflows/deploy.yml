name: Deploy VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy no VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 177.153.33.67
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /var/www/financascasa
            git pull
            npm install
            npm run build
            cp -r dist/* /var/www/financascasa/
            systemctl reload nginx
            echo "Deploy concluído!"
